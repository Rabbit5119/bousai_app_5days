# 指示・発信ボード実装依頼

あなたは既存システムを調査して実装するAI開発者です。
以下の要件をすべて満たす「指示・発信ボード」を、既存システムの技術・デザイン・認証・データ保存方式に合わせて実装してください。

## 実装方針

- まず既存の画面構成、ルーティング、認証、データモデル、保存処理、CSS規約を確認する。
- 既存機能や既存データを壊さず、指示・発信ボードに必要な範囲だけ変更する。
- 既存システムに相当する機能や部品がある場合は再利用する。
- サーバー側でも必須入力を検証し、ブラウザー側の検証だけに依存しない。
- 保存処理は既存のデータベースまたは永続化方式を使う。開発用の一時データだけで完了させない。
- 未認証ユーザーが利用できない既存仕様がある場合は、その仕様に従う。
- 実装後、画面表示、入力検証、保存、一覧反映、絞り込み、件数集計をテストする。

## 画面表示順

発信ボード画面は、必ず次の順番で表示する。

1. 現在の状況
2. 新規 指示・発信
3. 指示・発信一覧

## 1. 現在の状況

次の3項目を件数付きで表示する。

- 対応中 ○件
- 発信済 ○件
- 本日発信 ○件

件数は保存されているデータから動的に集計する。

- 「対応中」: 下書き保存されている件数
- 「発信済」: 公開済みで表示期限を過ぎていない指示・発信の件数。期限が「―」の場合は有効として数える
- 「本日発信」: 公開済みで、発信日時が現在の日付の件数

新しく「指示・発信する」で保存されたデータは、保存直後から件数に反映する。

## 2. 新規 指示・発信

入力フォームの見出しは「新規 指示・発信」とする。

### 災害名

必須入力。赤色の `＊` を付ける。
プルダウンの選択肢は次の7つとする。

- 津波
- 河川氾濫
- 道路冠水
- 土砂崩れ
- 積雪による道路遮断
- 獣害
- 山火事

初期状態は未選択の案内表示にする。

### 内容

必須入力。赤色の `＊` を付ける。
災害や指示・発信の詳細を入力できる複数行テキストボックスにする。

### 表示期限

任意入力。赤色の `＊` は付けない。

- 日付: カレンダーから選択できる入力
- 時刻: 時刻選択できる入力

日付または時刻が未入力の場合、期限は「―」として扱う。

### 関連する避難所

必須入力。赤色の `＊` を付ける。
避難所検索画面で表示される避難所データと同じデータソースを使い、登録済み避難所をプルダウンに表示する。
初期状態は未選択の案内表示にする。

### 種類

必須入力。赤色の `＊` を付ける。
次のラジオボタンから1つだけ選択できるようにする。

- 職員への指示
- 職員への情報発信
- 住民への情報発信

### 対象者

「住民への情報発信」を選択した場合だけ表示する。
赤色の `＊` を付け、表示時は1つ以上の選択を必須にする。
チェックボックスで複数選択できるようにする。

- 全住民
- 高齢者
- 子ども連れ
- 要配慮者
- 観光客
- 避難利用者

「住民への情報発信」以外を選んだ場合は非表示かつ保存対象外にする。

### 優先度

必須入力。赤色の `＊` を付ける。
次のラジオボタンから1つだけ選択できるようにする。

- 緊急
- 高
- 通常

初期選択は「通常」とする。

### 操作ボタン

フォームの最後に次の2つのボタンを配置する。

- 下書き保存
- 指示・発信する

「指示・発信する」を押した場合は、必須項目を検証してから保存する。
「下書き保存」は、必須項目が未入力でも、その時点までの内容を永続保存する。

## 必須入力エラー

「指示・発信する」を押した際、赤色の `＊` が付いた項目に未入力がある場合は、次の文言を画面に表示する。

> 選択していない必須項目があります。

エラー時は保存・発信を実行しない。

「下書き保存」を押した場合は、必須項目が空でもエラーを表示せず、入力済みの内容を保存する。

対象者は、種類が「住民への情報発信」の場合だけ必須とする。
表示期限は任意なので、未入力でもこのエラーを表示しない。

## 保存データ

「指示・発信する」が成功した場合、少なくとも次の情報を保存する。

- 災害名
- 内容
- 表示期限の日付と時刻
- 関連する避難所
- 種類
- 対象者
- 優先度
- 発信日時
- 状態

初期状態は次のルールにする。

- 「職員への指示」: 種類は「指示」、状態は「未確認」
- 「職員への情報発信」または「住民への情報発信」: 種類は「発信」、状態は「発信済」

## 下書きのライフサイクル

- 新規フォームの横に下書き一覧を表示する。
- 各下書きに「適用」と「削除」を表示する。
- 「適用」を押すと、保存済みの災害名、内容、表示期限、関連避難所、種類、対象者、優先度をフォームへ復元する。
- 下書きを適用して再度「下書き保存」した場合は、同じIDを上書きし、重複作成しない。
- 下書きを適用して「指示・発信する」した場合は、同じIDを公開データへ変更し、下書き一覧から削除する。
- 下書きの削除は確認後に永続データから削除する。
- 下書きは指示・発信一覧と現在の状況の「発信済」「本日発信」には含めない。

## 指示・発信一覧

新規フォームの下に、見出し「指示・発信一覧」を表示する。

一覧は次の列とする。

- 災害名
- 種類
- 対象
- 内容
- 表示期限
- 関連する避難所
- 優先度
- 状態
- 操作

「対象」欄は、種類が「住民への情報発信」の場合は選択した対象者を表示する。
対象者が複数の場合は、全住民、高齢者のように一覧表示する。
「職員への指示」「職員への情報発信」の場合は、対象欄を空欄にする。
実際の一覧は保存データから生成する。
「指示・発信する」で新規保存されたデータは、画面遷移または再読み込み後に一覧へ表示する。

一覧の「操作」には、保存済みの公開データに対して「編集」「削除」を表示する。

- 編集: 入力フォームへ値を復元し、更新後は同じIDを上書きする
- 削除: 確認後に永続データから削除する
- 初期サンプルを表示する場合でも、編集・削除の対象は保存済みデータだけにする

## 絞り込み

一覧の上部に、次の3つのプルダウンを配置する。

- 災害別
- 優先度別
- 対象者別

実装上は、災害名、優先度、対象者を絞り込めるようにする。
対象者別では、複数対象者を含む行も該当対象者を選んだ場合に表示する。
複数の絞り込みを選んだ場合は、すべての条件に一致する行だけを表示する。
一致する行がない場合は、次のメッセージを表示する。

> 条件に一致する指示・発信はありません。

## デザイン・操作性

- ホーム画面と同じデザインへ統一する。
- 青色ヘッダー、メニューアイコン、ドロップダウンメニューを使う。
- ホーム画面と同じ日本語フォントを使う。
- 白いカード、角丸、控えめな影、青色の見出し、同じ余白設計を使う。
- 指示・発信の機能や仕様を変えず、デザインだけを統一する。
- 必須マークは赤色で表示する。
- ラジオボタンは1つだけ、チェックボックスは複数選択可能にする。
- スマートフォンでも入力・一覧確認ができるようにする。
- 表が横に長くなる場合は横スクロール可能にする。
- 入力欄のラベルと `id` を関連付ける。
- 動的表示を使う場合も、キーボード操作とスクリーンリーダーに配慮する。

## 完了条件

次をすべて確認してから完了とする。

1. 画面の順番が「現在の状況」「新規 指示・発信」「指示・発信一覧」になっている。
2. 災害名、内容、関連する避難所、種類、優先度が必須になっている。
3. 必須項目には赤色の `＊` が付いている。
4. 表示期限だけは任意入力になっている。
5. 住民への情報発信を選んだ場合だけ対象者が表示され、1つ以上選択できる。
6. 「指示・発信する」で必須項目が不足している場合、指定文言が表示され、保存されない。
7. 「下書き保存」は必須項目が不足していても、入力済みの内容を保存する。
8. 下書きを適用して再保存すると上書きされ、公開すると下書き一覧から消える。
9. 下書きを削除できる。
10. 正常に発信すると永続保存される。
11. 保存直後に一覧へ追加される。
12. 保存直後に現在の状況の件数へ反映される。
13. 災害別、優先度別、対象者別の絞り込みが動作する。
14. 公開済みの編集・削除が動作する。
15. ホーム画面とボード画面のデザインが統一されている。
16. 既存の認証、避難所検索、その他の既存機能が壊れていない。
17. 関連するテストまたは同等の動作確認を実行し、結果を報告する。
